---
name: <Title Case Name>
description: >-
  <A summary of the WHOLE guide — what you'd know if you loaded the full file.
  ONE line by default; expand to at most 5 lines ONLY if the guide covers enough
  that a single line would mislead. This is the long form; the INDEX.md line is a
  shorter one-liner trigger derived from this. They stay consistent, not identical.>
last_verified: 2026-06-19
authored_with: claude-sonnet-4-6
last_verified_with: claude-sonnet-4-6
tags: [tag1, tag2]
---

# <Name>

> Copy this file to `docs/<kebab-case-name>.md` to start a new guide, then add a **one-line** trigger for it in `INDEX.md`.
> Keep one topic per guide. **Guides are living and will grow** — when a related learning arrives, append/merge it here (and widen the `description` + reconcile the INDEX line) rather than creating a near-duplicate guide. Edit in place; don't append contradictions.

## When this applies / Symptom

What task or error brings you here. Be concrete — this is how you (and a teammate) decide the guide is relevant.

## The fix / How it works

The actual procedure, command, or pattern. Include exact commands and file paths.

## Why (the part the code/diff can't show)

Intent, what was tried and rejected, the cost of getting it wrong. This is the durable value.

## References

- Repo paths, doc links, related guides via `docs/<other>.md`.
