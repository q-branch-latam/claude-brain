# Build Log — <PROJECT_NAME>

> Append-only running narrative (Context-Stack Layer 2). Newest entries at the bottom.
> The AI updates this autonomously on the triggers in CLAUDE.md — no need to ask.
> Durable, teachable gotchas get promoted to the shared brain via `/capture`; this
> log is the project-local story, including the messy parts (dead ends, surprises,
> where we left off). Terse bullets with a `[tag]`. Never log live secrets/tokens.

## <DATE>
- **[milestone]** Project bootstrapped: docs/, build-log, git, project CLAUDE.md created.
- **[next]** <what to do first>
